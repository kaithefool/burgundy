doctype html
html
  head
    link(rel='stylesheet', href=`/assets/commons.css?v=${version}`)
    script(src=`/assets/commons.js?v=${version}`)
    script#env(type='application/json')!= JSON.stringify(env)
    block head
  body
    block body
